import loginStyle from '../styles/Login.module.scss'

const Login = () => {
    return (
        <div className={loginStyle.auth_container}>
            <div className={loginStyle.auth_form_container}>
        
            </div>
        </div>
    );
};

export default Login;